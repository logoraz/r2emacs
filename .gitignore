# Root
/elpa*

# Modules
/modules/r2-secrets.el

## Extensions
/syntax/*.elc

## Contrib — vendored 3rd-party libraries kept as live git checkouts.
## We track the directory exists (via .gitkeep) but ignore the checkout
## contents entirely — each subdirectory is independently cloned and updated.
/contrib/*
!/contrib/.gitkeep
